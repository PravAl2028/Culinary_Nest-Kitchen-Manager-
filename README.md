# CulinaryNest Backend 🍽️

## Backend for Culinary_Nest built with:

- Node.js — runs the backend server
- Express.js — handles all API routes
- Mongoose — connects and talks to MongoDB
- CORS — allows frontend to call backend
- Dotenv — loads .env variables into the server
- Nodemon — auto-restarts server when files change

## Frontend for Culinary_Nest built with:
- React 19 — builds the entire UI
- TypeScript — adds type safety to JavaScript
- Vite 6 — runs dev server and builds the app
- Tailwind CSS — styling via CDN
- Lucide React — icons used throughout the app
- Google Gemini AI — AI recipe and meal suggestions

## Database

- MongoDB Atlas — cloud database storing all app data
- MongoDB Compass — visual tool to view database data

## Features
- Room-based cooking collaboration
- AI-generated weekly meal plans
- Recipe suggestions
- Shopping list automation
- Smart Picks
- Family Voting for Dish Approval


## Setup
```bash
- npm install
- npm run dev
- cd server && npm run dev — starts backend on port 5000